# 플러그인은 file://에서 돌아 location.origin을 쓸 수 없다.
# 어느 relay-service에 붙을지 빌드 시점에 박아 넣는다.
#
# 이 파일을 .env.production.local로 복사하고 주소를 자기 것으로 바꾼다.
# 같은 주소가 app.json의 network whitelist에도 있어야 한다. 한쪽만
# 고치면 붙지 않는다.
VITE_RELAY_URL=http://192.168.0.10:4100
